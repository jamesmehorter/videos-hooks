import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

// See https://videos-hooks-ebon-five.vercel.app/
// new in project: `$ vercel`
// deploy an update: `$ vercel --prod`

// OR

// See https://vibrant-archimedes-d63c67.netlify.app
// Installed into https://github.com/jamesmehorter/videos-hooks
// updates on master push
